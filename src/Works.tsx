import { Header } from "./component/Header";
import { Home } from "./Home";

export const Works = () => {
    return (
        <div>
            <Header mode={"works"} />
            <Home />
        </div>
    );
}