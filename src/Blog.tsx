import { useEffect } from "react";

export const Blog = () => {
    useEffect(() => {
        window.location.href = "https://blog.tb-works.com/";
    }, [])
    return (
        <div>
        </div>
    );
}
